import { EVENT_ACTIONS, EVENT_MODULES } from './event.types.js';

/**
 * Canonical event name map for the platform.
 *
 * Every value follows the `module.action` convention.
 *
 * @type {Readonly<Record<string, string>>}
 */
export const EVENT_NAMES = Object.freeze({
  APPLICATION_CREATED: `${EVENT_MODULES.APPLICATIONS}.${EVENT_ACTIONS.CREATED}`,
  APPLICATION_UPDATED: `${EVENT_MODULES.APPLICATIONS}.${EVENT_ACTIONS.UPDATED}`,

  CUSTOMER_CREATED: `${EVENT_MODULES.CUSTOMERS}.${EVENT_ACTIONS.CREATED}`,
  CUSTOMER_UPDATED: `${EVENT_MODULES.CUSTOMERS}.${EVENT_ACTIONS.UPDATED}`,

  DOCUMENT_CREATED: `${EVENT_MODULES.DOCUMENTS}.${EVENT_ACTIONS.CREATED}`,
  DOCUMENT_UPDATED: `${EVENT_MODULES.DOCUMENTS}.${EVENT_ACTIONS.UPDATED}`,
  DOCUMENT_UPLOADED: `${EVENT_MODULES.DOCUMENTS}.${EVENT_ACTIONS.UPLOADED}`,
  DOCUMENT_DOWNLOADED: `${EVENT_MODULES.DOCUMENTS}.${EVENT_ACTIONS.DOWNLOADED}`,

  PAYMENT_CREATED: `${EVENT_MODULES.PAYMENTS}.${EVENT_ACTIONS.CREATED}`,
  PAYMENT_COMPLETED: `${EVENT_MODULES.PAYMENTS}.${EVENT_ACTIONS.COMPLETED}`,
  PAYMENT_FAILED: `${EVENT_MODULES.PAYMENTS}.${EVENT_ACTIONS.FAILED}`,

  INVOICE_CREATED: `${EVENT_MODULES.INVOICES}.${EVENT_ACTIONS.CREATED}`,
  INVOICE_UPDATED: `${EVENT_MODULES.INVOICES}.${EVENT_ACTIONS.UPDATED}`,
  INVOICE_COMPLETED: `${EVENT_MODULES.INVOICES}.${EVENT_ACTIONS.COMPLETED}`,

  COUNTRY_CREATED: `${EVENT_MODULES.COUNTRIES}.${EVENT_ACTIONS.CREATED}`,
  COUNTRY_UPDATED: `${EVENT_MODULES.COUNTRIES}.${EVENT_ACTIONS.UPDATED}`,

  VISA_TYPE_CREATED: `${EVENT_MODULES.VISA_TYPES}.${EVENT_ACTIONS.CREATED}`,
  VISA_TYPE_UPDATED: `${EVENT_MODULES.VISA_TYPES}.${EVENT_ACTIONS.UPDATED}`,
  VISA_TYPE_IMPORTED: `${EVENT_MODULES.VISA_TYPES}.${EVENT_ACTIONS.IMPORTED}`,
  VISA_TYPE_EXPORTED: `${EVENT_MODULES.VISA_TYPES}.${EVENT_ACTIONS.EXPORTED}`,

  USER_CREATED: `${EVENT_MODULES.USERS}.${EVENT_ACTIONS.CREATED}`,
  USER_UPDATED: `${EVENT_MODULES.USERS}.${EVENT_ACTIONS.UPDATED}`,
  USER_ASSIGNED: `${EVENT_MODULES.USERS}.${EVENT_ACTIONS.ASSIGNED}`,

  AUTH_CREATED: `${EVENT_MODULES.AUTH}.${EVENT_ACTIONS.CREATED}`,
  AUTH_FAILED: `${EVENT_MODULES.AUTH}.${EVENT_ACTIONS.FAILED}`,

  WORKFLOW_STARTED: `${EVENT_MODULES.WORKFLOW}.started`,
  WORKFLOW_COMPLETED: `${EVENT_MODULES.WORKFLOW}.${EVENT_ACTIONS.COMPLETED}`,

  NOTIFICATION_SENT: `${EVENT_MODULES.NOTIFICATIONS}.sent`,
  NOTIFICATION_COMPLETED: `${EVENT_MODULES.NOTIFICATIONS}.${EVENT_ACTIONS.COMPLETED}`,

  TIMELINE_RECORDED: `${EVENT_MODULES.TIMELINE}.recorded`,
  TIMELINE_CREATED: `${EVENT_MODULES.TIMELINE}.${EVENT_ACTIONS.CREATED}`,

  AI_ANALYSIS_COMPLETED: `${EVENT_MODULES.AI}.analysis.completed`,
  AI_ANALYSIS_FAILED: `${EVENT_MODULES.AI}.analysis.failed`,

  SYSTEM_STARTED: `${EVENT_MODULES.SYSTEM}.started`,
  SYSTEM_FAILED: `${EVENT_MODULES.SYSTEM}.${EVENT_ACTIONS.FAILED}`,
});

/**
 * Frozen helper collections grouped by module.
 *
 * @type {Readonly<string[]>}
 */
export const SYSTEM_EVENTS = Object.freeze([
  EVENT_NAMES.SYSTEM_STARTED,
  EVENT_NAMES.SYSTEM_FAILED,
]);

/**
 * Frozen helper collections grouped by module.
 *
 * @type {Readonly<string[]>}
 */
export const APPLICATION_EVENTS = Object.freeze([
  EVENT_NAMES.APPLICATION_CREATED,
  EVENT_NAMES.APPLICATION_UPDATED,
]);

/**
 * Frozen helper collections grouped by module.
 *
 * @type {Readonly<string[]>}
 */
export const CUSTOMER_EVENTS = Object.freeze([
  EVENT_NAMES.CUSTOMER_CREATED,
  EVENT_NAMES.CUSTOMER_UPDATED,
]);

/**
 * Frozen helper collections grouped by module.
 *
 * @type {Readonly<string[]>}
 */
export const DOCUMENT_EVENTS = Object.freeze([
  EVENT_NAMES.DOCUMENT_CREATED,
  EVENT_NAMES.DOCUMENT_UPDATED,
  EVENT_NAMES.DOCUMENT_UPLOADED,
  EVENT_NAMES.DOCUMENT_DOWNLOADED,
]);

/**
 * Frozen helper collections grouped by module.
 *
 * @type {Readonly<string[]>}
 */
export const PAYMENT_EVENTS = Object.freeze([
  EVENT_NAMES.PAYMENT_CREATED,
  EVENT_NAMES.PAYMENT_COMPLETED,
  EVENT_NAMES.PAYMENT_FAILED,
]);

/**
 * Frozen helper collections grouped by module.
 *
 * @type {Readonly<string[]>}
 */
export const WORKFLOW_EVENTS = Object.freeze([
  EVENT_NAMES.WORKFLOW_STARTED,
  EVENT_NAMES.WORKFLOW_COMPLETED,
]);

/**
 * Frozen helper collections grouped by module.
 *
 * @type {Readonly<string[]>}
 */
export const NOTIFICATION_EVENTS = Object.freeze([
  EVENT_NAMES.NOTIFICATION_SENT,
  EVENT_NAMES.NOTIFICATION_COMPLETED,
]);

/**
 * Frozen helper collections grouped by module.
 *
 * @type {Readonly<string[]>}
 */
export const TIMELINE_EVENTS = Object.freeze([
  EVENT_NAMES.TIMELINE_RECORDED,
  EVENT_NAMES.TIMELINE_CREATED,
]);

/**
 * Frozen helper collections grouped by module.
 *
 * @type {Readonly<string[]>}
 */
export const AI_EVENTS = Object.freeze([
  EVENT_NAMES.AI_ANALYSIS_COMPLETED,
  EVENT_NAMES.AI_ANALYSIS_FAILED,
]);

/**
 * Frozen helper collections grouped by module.
 *
 * @type {Readonly<string[]>}
 */
export const USER_EVENTS = Object.freeze([
  EVENT_NAMES.USER_CREATED,
  EVENT_NAMES.USER_UPDATED,
  EVENT_NAMES.USER_ASSIGNED,
]);

/**
 * Immutable bundle of event constants for shared consumption.
 *
 * @type {Readonly<{
 *   EVENT_NAMES: typeof EVENT_NAMES,
 *   SYSTEM_EVENTS: typeof SYSTEM_EVENTS,
 *   APPLICATION_EVENTS: typeof APPLICATION_EVENTS,
 *   CUSTOMER_EVENTS: typeof CUSTOMER_EVENTS,
 *   DOCUMENT_EVENTS: typeof DOCUMENT_EVENTS,
 *   PAYMENT_EVENTS: typeof PAYMENT_EVENTS,
 *   WORKFLOW_EVENTS: typeof WORKFLOW_EVENTS,
 *   NOTIFICATION_EVENTS: typeof NOTIFICATION_EVENTS,
 *   TIMELINE_EVENTS: typeof TIMELINE_EVENTS,
 *   AI_EVENTS: typeof AI_EVENTS,
 *   USER_EVENTS: typeof USER_EVENTS
 * }>}
 */
export const PLATFORM_EVENT_CONSTANTS = Object.freeze({
  EVENT_NAMES,
  SYSTEM_EVENTS,
  APPLICATION_EVENTS,
  CUSTOMER_EVENTS,
  DOCUMENT_EVENTS,
  PAYMENT_EVENTS,
  WORKFLOW_EVENTS,
  NOTIFICATION_EVENTS,
  TIMELINE_EVENTS,
  AI_EVENTS,
  USER_EVENTS,
});

export default Object.freeze({
  EVENT_NAMES,
  SYSTEM_EVENTS,
  APPLICATION_EVENTS,
  CUSTOMER_EVENTS,
  DOCUMENT_EVENTS,
  PAYMENT_EVENTS,
  WORKFLOW_EVENTS,
  NOTIFICATION_EVENTS,
  TIMELINE_EVENTS,
  AI_EVENTS,
  USER_EVENTS,
  PLATFORM_EVENT_CONSTANTS,
});
