services:
  platform:
    build:
      context: .
      target: dev
    command: pnpm dev
    env_file:
      - .env
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules
